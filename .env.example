# ─── LLM: Anthropic Claude (you have credits) ───────────────────────────────
# https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# ─── Embeddings: Google Gemini (free tier, no card, 1,500 req/day) ──────────
# https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# ─── Vector store + DB: Supabase (free tier) ────────────────────────────────
# Project settings → API
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Service-role key: server-only, bypasses RLS. NEVER expose to the browser.
SUPABASE_SERVICE_ROLE_KEY=

# ─── Optional (Milestone: provider A/B) ─────────────────────────────────────
# Cloudflare Workers AI — swap-in embedding provider to prove the abstraction.
# https://dash.cloudflare.com → Workers AI
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_API_TOKEN=
